apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceRouter
metadata:
  name: web
  namespace: web
spec:
  routes:
    - match:
        http:
          pathPrefix: /
      destination:
        requestTimeout: 5s  #total time permitted for the entire downstream request to be processed, including retry attempts.